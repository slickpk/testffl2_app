<h1>New teamplayer</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@teamplayer) do |t| %>
			<%= t.label :playerid %>
			<%= t.text_field :playerid %>

			<%= t.label :teamid %>
			<%= t.text_field :teamid %>

			<%= t.label :live_player_id %>
			<%= t.text_field :live_player_id %>

			<%= t.label :fteam_id %>
			<%= t.text_field :fteam_id %>

			<%= t.submit "Create my team", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>

<%#= render 'form' %>

<%= link_to 'Back', teamplayers_path %>
