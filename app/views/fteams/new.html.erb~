<h1>Add Team Page</h1>
<div class="row">
	<div class="span6 offset3">
		<%= form_for(@fteam) do |t| %>
			<%= t.label :Team %>
			<%= t.text_field :Team %>

			<%= t.label :TeamUrl %>
			<%= t.text_field :TeamUrl %>
			<%= t.submit "Create my team", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>

<%#= render 'form' %>

<%= link_to 'Back', fteams_path %>
